# Phenomenology audit — 2026-09-22

**Claim level:** ≤ 2  
**experimental_validation / thrust_validated / energy_extraction_validated:** false  
Does not change \(W_\star\) and does not unfreeze Stage 1.

## 1. Two symbols (do not fuse)

| Symbol | Definition | Role |
|--------|------------|------|
| \(W_\star=1/(4\pi)\approx 0.079577\) | galactic / Einstein-equation coupling anchor | Option A |
| \(W(n)=0.08\,e^{0.23(n-3)}\) | engineering recursion, pins \(W(3)=0.08\) | M2 / residual-force chain |

Relative difference \(|1/(4\pi)-0.08|/0.08\approx 0.53\%. That is a rounding coincidence under axioms E1–E4, not a measurement and not a gasket eigenvalue.

Deprecated indexing \(W(n)=0.08\,e^{0.23(n-1)}\) gives \(W(3)\approx 0.1267\), which collides with the model-internal bound \(W<0.125\). Option A forbids inserting M2 into gravitational formulae (`W3_RESOLUTION_NOTE.md`, `killgate_verification.py`).

## 2. What is actually derived vs assumed

| Claim | Status |
|-------|--------|
| \(1/(4\pi)\) from solid-angle axioms E1–E4 | Conditional derivation. Axioms are Model class. |
| Tree-level screen matching \(c_\star=1\Rightarrow W_{\rm eff}=1/(4\pi)\) | Matching convention. Bulk\to boundary not closed. |
| Finite-mesh spectral ratios = 0.08 | **Failed** (`WSTAR_FIRST_PRINCIPLES_NOTE.md`: \(\lambda_1/\lambda_N=0.029\)) |
| Gasket \(\lambda_{\max}=6\) or Kigami 3/5 produces \(W_\star\) | **Rejected** (wrong problem) |
| SPARC median \(\chi^2_{\rm red}\sim 9.1\) | Honest residual. Not \(\mathcal{O}(1)\). Not a pass. |
| Macro SPARC / \(r_0\propto M_b^{0.40}\) | Repo-reported; not re-certified this pass |
| Lensing \(\delta_{\rm sat}=1.2\) | Phenomenological cap. Not derived from the metric. |
| Bullet lag \(r_0/c\) | Repo: simple model **FAIL** |
| Ghost-free \(n\le 4\) | Model-internal from \(W(n)<0.125\). Not a propagator theorem. |
| Thrust \(3\times 10^{-8}\,\mathrm{N/W}\) | Design target only. `THRUSTER_VS_CFT_BOUNDARY.md`. |

## 3. Older narrative vs later locks

`ware-constant-phenomenology.md` still states deprecated M2 indexing, hybrid ratios 0.795/1/1.993, and thrust calibration from W(3). Those sentences are **superseded** by Sweep-137 / CLAIM_STATUS / this file. CLAIM_STATUS is authoritative.

## 4. Weak-field template (as written, not as GR)

$$
a_{\rm tot}=\frac{GM_b}{r^2}+\frac{W_\star GM_b}{r_0 r},\qquad
v_\infty^2=\frac{W_\star GM_b}{r_0}.
$$

This is a two-parameter acceleration law once \(r_0(M_b)\) is chosen. Solar-system ratio in the note,
\(\eta\approx W_\star r_{\rm AU}/r_0\sim 5\times 10^{-11}\) for the quoted \(r_0\), is an order-of-magnitude screening statement, not a Cassini analysis performed here.

## 5. Allowed next work in this repo

1. Keep \(W_\star\) and \(W(n)\) in separate code paths (already Option A).
2. Publish SPARC residuals without calling \(\chi^2\sim 9\) a pass.
3. Derive or drop \(\delta_{\rm sat}\) and Bullet Model D from the written action.
4. Do not import print-skew \(F\) or gasket spectrum as a Ware derivation.

```
experimental_validation     = false
thrust_validated            = false
energy_extraction_validated = false
```
